[project]
name = "life-event-detection-benchmark"
version = "0.1.0"
description = "Skeleton for implicit life event detection benchmark in banking chat histories."
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
